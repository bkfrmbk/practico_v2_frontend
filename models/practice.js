class Practice {
    constructor(id, title, goal, time_type, total_duration, current_period_duration) {
        this.id = id;
        this.title = title;
        this.goal = goal;
        this.time_type = time_type;
        this.total_duration = total_duration;
        this.current_period_duration = current_period_duration;
    }
}

export default Practice;